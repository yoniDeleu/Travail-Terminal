from file import *
from pile import *
"""
#Exo 2
def inverse(file):
    p=pile()
    while not f.est_vide():
        p.empile(f.defile)
    while not p.est_vide():
        file.enfile(p.depile)
    return file
#Exo 3
def separe (f):
    paire=File()
    impaire=File()
    while not f.est_vide ():
        x = f.defile()
        if x % 2 == 0:
            paire.enfile(x)
        else:
            impaire.enfile(x)
    return paire, impaire

#Exo 5
def supr(f):
    t=f.taille()
    f2=File()
    for i in range (t-1):
        f2.enfile(f.defile())
    return f2
"""

#Exo 6